# Example environment variables
# Copy to `.env` and adjust as needed for your environment.
# For CI, set the same variable in your pipeline environment.
BASE_URL=https://jsonplaceholder.typicode.com/todos
